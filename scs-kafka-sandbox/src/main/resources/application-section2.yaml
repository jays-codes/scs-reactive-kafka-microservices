# --- with producer
spring:
  application:
    name: scs-kafka-sandbox
  cloud:
    function:
      definition:
        producer;consumer
    stream:
      bindings:
        producer-out-0:
          destination: input-topic
        consumer-in-0:
          destination: input-topic
          group: some-group
